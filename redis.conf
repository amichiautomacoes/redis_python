protected-mode yes
timeout 0
tcp-keepalive 300

# Persistence defaults. Can be overridden at startup via CLI flags.
appendonly yes
appendfsync everysec

# Keep logs in stdout for container visibility.
loglevel notice
